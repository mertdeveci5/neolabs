[project]
name = "neolabs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "liteparse>=2.0.6",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
